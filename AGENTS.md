# AGENTS.md

## Project

- **servipet** — Python project (no framework or package manager chosen yet).
- Status: empty scaffold. Only a `.gitignore` exists on `main`.

## Setup

No dependencies or toolchain configured. Before writing code, the project needs:
- A package manifest (`pyproject.toml`, `requirements.txt`, or similar).
- A linter/formatter (`.gitignore` includes `.ruff_cache/`, suggesting Ruff is intended).
- A test runner if tests are planned.

## Conventions

None established. Watch for conventions as code lands and update this file.
